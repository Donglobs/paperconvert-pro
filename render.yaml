services:
  - type: web
    name: paperconvert-pro
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 2 --timeout 180
    healthCheckPath: /api/health
    autoDeploy: true
